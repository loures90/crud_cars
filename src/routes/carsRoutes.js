import express from 'express';

export const carsRoutes = express.Router()

// carsRoutes.get('/', listCars)
// carsRoutes.post('/', saveCar)
// carsRoutes.patch('/:id', updateCars)
// carsRoutes.get('/:id', getCar)
// carsRoutes.delete('/id',deleteCar)
